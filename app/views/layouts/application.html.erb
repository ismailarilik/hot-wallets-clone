<!DOCTYPE html>
<html>
  <head>
    <title>HOTWallets Clone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="192x192" href="/android-chrome-192x192.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/manifest.json">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#c026d3">
    <meta name="msapplication-TileColor" content="#c026d3">
    <meta name="msapplication-TileImage" content="/mstile-144x144.png">
    <meta name="theme-color" content="#c026d3">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="border-b px-6 py-3">
      <div class="container mx-auto flex items-center">
        <!-- Logo and title -->
        <%= link_to root_path, class: 'flex items-center' do %>
          <div>
            <%= image_tag 'logo.svg', alt: 'Logo' %>
          </div>
          <div class="ml-2 text-3xl text-fuchsia-600 font-semibold" id="brand">
            HOTWallets
          </div>
        <% end %>

        <!-- Wallets button -->
        <div>
          <%= link_to 'Wallets', wallets_path, class: 'ml-8 text-2xl font-middle' %>
        </div>
        <!-- Categories button -->
        <div>
          <%= link_to 'Categories', categories_path, class: 'ml-8 text-2xl font-middle' %>
        </div>

        <!-- Sign out button -->
        <% if current_user %>
          <div class="ml-auto">
            <%= button_to 'Sign out', destroy_user_session_path, method: :delete, class: 'text-gray-700' %>
          </div>
        <% end %>
      </div>
    </header>

    <main class="container mx-auto mt-28 mb-6 px-5 flex flex-col items-center">
      <% if notice.present? %>
        <div>
          <p
            class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block"
            id="notice"
          >
            <%= notice %>
          </p>
        </div>
      <% elsif alert.present? %>
        <div>
          <p
            class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block"
            id="alert"
          >
            <%= alert %>
          </p>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
